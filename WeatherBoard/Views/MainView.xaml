<Window x:Class="WeatherBoard.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WeatherBoard.Views"
         xmlns:vm="clr-namespace:WeatherBoard.ViewModels"
        mc:Ignorable="d"
        Title="MainView" Height="450" Width="800">
    <Window.DataContext>
        <vm:MainViewModel>
        </vm:MainViewModel>
    </Window.DataContext>
    <Grid>
        <Button Width="200" Height="30" Command="{Binding ShowWeather}">Show Weather</Button>
        <Label Width="200" Height="30" Content="{Binding Temperature}" Margin="296,245,296,143.4"></Label>
        <Label Width="200" Height="30" Content="{Binding Comment }" Margin="297,305,296.6,85"></Label>
        <TextBox Width="200" Height="30" Text="{Binding CityInput}" Margin="297,113,296.6,277"></TextBox>
    </Grid>
</Window>
